const noteInitialState = {}

const noteReducer = (state=noteInitialState, action)=>{
    switch(action.type) {
        default: {
            return {...state}
        }
    }
}

export default noteReducer