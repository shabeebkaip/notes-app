const userInialState = []

const userReducer = (state = userInialState, action)=>{
    switch(action.type) {
        default : {
            return [...state]
        }
    }
}
export default userReducer